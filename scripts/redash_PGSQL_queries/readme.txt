This directory contains all the SQL queries that are used on www.batteryarchive.org

To import/export the queries use the commands below

python3 query_import.py --redash-url "<your URL>" --api-key "<your key>"
python3 query_export.py --redash-url "<your URL>" --api-key "<your key>"

The API key can be found in your redash installation
